<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Spectrum Android App

    This file defines the app's network security policy:
    - Enforces HTTPS for all connections in production
    - Allows cleartext traffic only for local development
    - Pins certificates for enhanced security (optional, configure as needed)
-->
<network-security-config>
    <!-- Base configuration for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug configuration (allows localhost for development) -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user-added CAs for debugging -->
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Domain-specific configuration -->
    <!-- Uncomment and configure for production API endpoints -->
    <!--
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.spectrum.com</domain>
        <pin-set expiration="2025-12-31">
            <pin digest="SHA-256">YOUR_CERTIFICATE_PIN_HERE</pin>
        </pin-set>
    </domain-config>
    -->

    <!-- Allow localhost for React Native development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">10.0.3.2</domain>
    </domain-config>
</network-security-config>
